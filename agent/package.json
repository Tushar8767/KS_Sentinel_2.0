{
  "name": "ks-sentinel-agent",
  "version": "2.0.0",
  "private": true,
  "description": "KS Sentinel 2.0 — Local Sentinel Agent (Windows Host Service)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}

